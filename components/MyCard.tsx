import { Card } from "react-bootstrap";

export type Props = {
  source?: string;
  alt?: string;
  title?: string;
  content?: string;
};

export default function MyCard({ Props }) {
  return <></>;
}
